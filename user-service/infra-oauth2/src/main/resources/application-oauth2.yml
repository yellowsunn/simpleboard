spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${google-client-id}
            client-secret: ${google-client-secret}
            scope:
              - profile
              - email
---
spring:
  profiles:
    active: local

redirect:
  origin: http://localhost:3000

